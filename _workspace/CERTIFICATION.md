# 완료 인증 기준 — code-navigator-mac

> **소유자: 리더 단독.** 팀은 이 파일을 편집하지 않는다. 읽고 기준으로 삼으면 된다.
>
> 빌드 '완료'는 **리더가 아래를 전부 통과시키고 `BUILD_COMPLETE.md` 를 쓴 시점에만** 성립한다.
> 매트릭스·QA 리포트·작업표의 완료 문구는 *리더 인증 대기 중인 주장*이지 완료가 아니다.

## 0-A. 🔴 열린 결함 — 이게 닫히기 전에는 완료가 아니다

| # | 결함 | REQ | 상태 |
|---|------|-----|------|
| D-1 | **표준 모드에서 메뉴 편집 명령 8개가 버퍼를 오염시킨다** (`save`·`undo`·`redo`·`cut`·`copy`·`paste`·`selectAll`·`navigateForward`) | REQ-010 AC-2 | 계약 대기 |
| — | `navigateBack` | REQ-005 AC-4 | ✅ 닫힘 — `jumpBack()`(엔진 `normal!` 래핑, 모드 무관) |

**⌘S 는 나머지와 급이 다르다** — 다른 것들은 버퍼를 눈에 띄게 오염시키지만 **⌘S 는 성공처럼
보이는 실패**(저장했다고 믿는데 저장되지 않음)라 데이터 유실이다.

프론트가 남은 손상을 `knownModeUnsafeCommands` 집합으로 코드에 명시했다 — **목록이 바뀌면
테스트가 알려주고, 비면 지우라고 적혀 있다.** 매트릭스에도 REQ-010 AC-2 를 `FAIL(알려진 갭)`
으로 적었다. **통과로 적지 않은 것이 맞다.**

## 0. 발주 전제 (넷 다 충족돼야 최종 라운드를 발주한다)

| # | 전제 | 확인 방법 | 현재 |
|---|------|----------|------|
| 1 | 게이트 그린 | `bash _workspace/gate.sh` → `GATE: PASS` | ✅ 23:2x 리더 실측 (709건) |
| 2 | `.app` 조립·실행 | 게이트 프론트 블록 (`rootView=laidOut subviews=7`) | ✅ 23:2x 리더 실측 |
| 3 | **화면 잠금 해제** | `ioreg -n Root -d1 -a \| grep -A1 CGSSessionScreenIsLocked` → `<false/>` 또는 키 부재 | ❌ **유일한 남은 블로커 — 사용자 조치 대기** |
| 4 | **미연결 뷰 연결** | 아래 §1 마운트 카운트 | ✅ 23:3x — 게이트 22종 전부 마운트 + **리더가 T6 변이로 검사기 방어선 직접 실측** |

3번이 없으면 라이브 E2E·디자인 충실도를 **실행할 수 없다**. 4번이 없으면 **검증할 대상이 화면에 없다** —
플레이스홀더는 클릭할 수 없다.

## 1. 마운트 카운트 (기계적 — 화면 판정의 1차 관문)

정의부를 뺀 참조 수를 센다:

```bash
for v in PlaceholderPane MainWindowView StatusBarView ToolbarView FileTreeView EditorGridView \
         ProjectOpenView DefinitionCandidatesView EditSessionOverlayView ReferencePanelView \
         TextSearchPanelView SidePanelView; do
  n=$(grep -rn "$v(" Sources/ --include="*.swift" | grep -v "struct $v" | wc -l | tr -d ' ')
  printf "%-26s %s\n" "$v:" "$n"
done
```

**합격선: `PlaceholderPane` 이 0 이고, 나머지 화면 뷰가 각각 1 이상.**

**23:03 실측 — 미연결 9종.** 창에는 여전히 회색 판 3장이다.

| 미연결 뷰 | 막힌 요구사항 |
|---|---|
| **`EditorGridView`** | **REQ-004 AC-2 — 에디터 화면 그 자체(본체)** |
| `FileTreeView` | REQ-003 (파일 트리 전부) |
| `ProjectOpenView` | REQ-001 (프로젝트 열기) |
| `ReferencePanelView` | REQ-006 (참조 패널) |
| `TextSearchPanelView` | REQ-008 (전문 검색) |
| `DefinitionCandidatesView` | REQ-005 AC-2 (동명 정의 후보) |
| `EditSessionOverlayView` | REQ-004 AC-5 (편집 세션 끊김) |
| `ToolbarView` · `SidePanelView` | 셸 구성요소 |

**23:14 갱신 — 조립이 대부분 끝났다.** `PlaceholderPane` **0**, 위 표의 뷰는 전부 마운트됐다.
남은 미마운트는 셋:

| 미마운트 | 막힌 요구사항 |
|---|---|
| `IndexStatusChipView` | REQ-009 · REQ-002 AC-4 (인덱스 상태 칩) |
| `SymbolSearchModalView` | REQ-007 (심볼 퍼지 검색) |
| `ShellSplitter` | **REQ-011 AC-3 — 드래그 리사이즈 + 비율 재시작 복원** |

`ShellSplitter` 는 게이트 마운트 목록에도 없었고, 레포 전체에서 `DragGesture` 와 분할 비율
로직을 가진 파일이 그것 하나다. 즉 **지금 창은 영역 크기를 조절할 수 없다.**

이 값이 필요한 이유: 뷰가 컴파일되고 테스트를 통과해도 **아무도 인스턴스화하지 않으면
사용자에게 도달하지 않는다**. 그리고 그 상태에서도 게이트는 초록이다. 실제로 630여 테스트가
통과하는 동안 창은 회색 판 3장이었고, 그 사이 미연결 뷰는 5종에서 9종으로 **늘었다**.

**병목은 만드는 것이 아니라 꽂는 것이다.** 조립 지점(`MainWindowView`)은 프론트 시니어 소유다.

⚠ 마운트 카운트는 *꽂혔는지*만 본다. *제대로 그려지는지*는 아래가 본다.

### 검사기 사각 4건 — QA 실측 (23:28, 격리 복사본에서 변이)
발견 기반으로 뒤집은 방향은 옳지만 **발견 정규식이 좁아 침묵이 되돌아왔다.** 넷 다 조용히 통과한다:

| # | 변이 | 왜 새는가 |
|---|---|---|
| T3 | 이름에 **숫자**(`QaPane2View`) | 발견 정규식이 `struct [A-Za-z]+` — 숫자·`_` 불포함 |
| T4 | **`private`/`internal`/`fileprivate`** 선언 | `^(public )?struct` — 다른 접근 수식어를 못 받는다 |
| T5 | **중첩·들여쓰기** 선언 | `^` 앵커라 컬럼 0만 본다 |
| T6 | **자기 파일 `#Preview`에서만 참조** | 같은 파일 참조를 마운트로 센다 |

**T6 이 가장 위험하다.** SwiftUI 에서 `#Preview` 는 관용구다 — **미마운트 뷰에 프리뷰를 하나
붙이는 순간 검사기가 그 뷰를 놓친다.** 리더의 독립 열거도 같은 T6 을 갖고 있었다(선언 줄만
제외했다). 그러므로 **현재의 "미마운트 0"은 T6 을 고친 뒤 재확인해야 한다.**

정상 동작은 확인됐다: 발견 0건 → `FAIL: 뷰를 하나도 찾지 못했다` exit 1 · 평범한 미마운트 →
FAIL · 고아 클러스터 → 부모가 FAIL · 원복 → exit 0.

**→ 수정 완료·리더 재확인 (23:3x).** 발견 정규식이 접근 수식어·숫자·중첩·다중 프로토콜을 받고,
`#Preview` 블록만 중괄호 깊이로 파싱해 제외한다(파일 전체가 아니다 — **같은 파일의 정당한
참조는 살린다**: `private` 하위 뷰가 그 파일의 마운트된 부모에게 쓰이는 것은 실제로 도달 가능하다).
리더가 **격리 복사본에서 T6 을 직접 변이**했다: 기준선 22종 exit 0 → `#Preview` 에서만 참조되는
뷰 추가 시 `FAIL … 사용자에게 도달할 수 없다` exit 1 → 원복 시 exit 0. **방어선 실재 확인.**

⚠ **리더의 임시 검사 쪽이 틀렸다.** 내가 급히 만든 열거는 *선언 파일 전체*를 제외해서
`IndexDetailsPopover`(같은 파일의 마운트된 부모가 쓰는 `private` 하위 뷰)를 미마운트로
오판했다. 게이트 쪽이 옳다. **급조한 검사와 게이트가 갈리면 급조한 쪽을 먼저 의심한다.**

### 더 큰 맹점 — 마운트 검사기는 **뷰만** 본다
실측으로 드러났다: `Menu/AppMenuBuilder` · `MenuDescriptor` 가 잘 만들어져 있는데 **그것으로
`NSMenu` 를 지어 설치하는 코드가 0줄이었고, `NSApp.mainMenu` 가 한 번도 설정되지 않았다.**
마운트 검사기는 `MenuBarController` 가 `View` 가 아니라서 발견 대상이 아니다.

결과가 뷰 미마운트보다 나쁘다: 메뉴가 없으면 AppKit 이 ⌘ 조합을 아무도 claim 하지 않은 채
뷰의 `keyDown` 으로 떨어뜨리고, **⌘O·⌘P·⇧⌘F 가 `<D-o>`·`<D-p>`·`<D-F>` 로 Neovim 에
전달된다.** REQ-011 AC-2("Neovim 입력과 충돌하지 않는다")가 정면으로 깨진다.

→ **"만들어 놓고 안 연결"은 뷰만의 문제가 아니다.** 컨트롤러·설치자·라우터처럼 화면이 아닌
구성요소도 같은 형태로 샌다. 인증에서는 뷰 마운트 카운트만 보지 말고 **`--self-check` 의
`menus=` 보고처럼 "실제로 설치·동작했는가"를 앱이 스스로 보고하게 한 것**을 함께 본다.
현재 게이트: `rootView=laidOut subviews=7 menus=7` (`verify-bundle.sh` 가 메뉴 6개 이상을 요구).

### 이 지표의 맹점 (쓰기 전에 알아야 한다)
`Type(` 형태의 **생성자 호출만** 잡는다. 정적 멤버로만 쓰이는 타입은 실제로 쓰이는데도 0 으로
나온다 — 실측: `PanelTint`(`PanelTint.warning.dynamicColor` 로 2곳에서 사용) ·
`CodeNavigatorApplication`(`CodeNavigatorApplication.run()`). **0 을 미사용으로 읽기 전에
정적 멤버 사용을 확인한다.**

그리고 반대 방향의 맹점이 더 위험하다: **검사 목록이 손으로 관리되면 새 뷰는 기본적으로
검사에서 빠진다.** 실제로 `IndexStatusChipView` · `SymbolSearchModalView` · `ShellSplitter`
셋이 미마운트인데 목록에 없어 게이트가 통과했다. 목록은 **기본 포함**이어야 한다 —
`View/` 의 뷰 타입을 전부 열거해 마운트를 요구하고, 의도적 예외만 명시적 허용목록에 둔다.
(그 때 `PanelTint` 같은 정적 전용 타입은 허용목록에 넣되 *이유를 적는다*.)

### 이미 있는 렌더링 계기 (QA 정정 반영)
`GridRenderingTests` 는 **오프스크린 비트맵에 실제로 그리고 픽셀을 샘플링**한다 — 순수 함수
테스트가 아니다. 더블폭 문자·`startColumn`·커서 폭이 픽셀 수준 방어선을 갖는다(변이 실측 확인).
**화면 잠금과 무관하게 돈다.**
→ 그러므로 "렌더링을 재는 계기가 하나도 없다"는 틀렸다. **없는 것은 합성 층의 계기** —
어떤 뷰가 실제로 창에 꽂히는가. 마운트 카운트가 그 자리를 메운다.

## 2. 게이트 (리더가 직접 재실행 — 팀 주장이 아니라)

```bash
bash _workspace/gate.sh
```

포함: `swift build` · `swift test --no-parallel` · 실행 테스트 수 하한 · `.app` 조립 ·
번들 실행 검증 · 번들 검사기 자체 검사 · 민감정보 스캔.

**`--no-parallel` 인 이유**: 다섯 스위트가 각자 실제 Neovim 을 띄워, 병렬에서는 같은 코드가
부하에 따라 통과하기도 실패하기도 했다(5회 중 3회 실패 → 직렬 3회 전부 통과).

여기에 **뷰 조립 방어선**이 더해져야 한다(프론트 시니어 작업):
뷰 인스턴스화 테스트 + 조립 지점 고정. 단언 대상은 **"플레이스홀더가 아니다"** 여야 한다 —
"뷰가 만들어졌다"만 단언하면 플레이스홀더가 남아 있어도 통과한다.

## 3. 라이브 E2E — 실제 `.app` 을 띄워서

### 인증 때 오해하지 말 것 (미리 확인해 둔 정상 동작)
- **참조 검색 결과에 `README.md` 가 섞여 나온다** — 정상이다. 이름 기반 검색이라 문서의 같은
  이름도 참조로 잡힌다. 결함으로 보고하지 마라.
- 픽스처의 `안내문` 문장에는 **홀로 선 `Index` 토큰이 없다**(backend-junior 가 제거). 있으면
  `Index` 검색에 그 줄이 정당하게 걸려서 "단어 경계 규칙이 깨졌다"로 오독된다.

### 대상 프로젝트 (미리 정해 둔다 — 인증 때 고르면 멈춘다)
지원 언어는 **Kotlin · Java · TypeScript · JavaScript** 다. **Swift 는 지원하지 않으므로 이
레포 자신을 열어서는 E2E 를 할 수 없다.**

| 용도 | 프로젝트 | 근거 |
|---|---|---|
| 시나리오 정밀 검증 | `_workspace/e2e-fixture/` (리더가 만든 픽스처) | SC-1 `SymbolIndex` 1곳 · SC-2 `resolveTarget` **3곳** · 한글 줄 · `node_modules` 제외 확인 |
| 실제 규모·성능 | `/Users/skull/Documents/repo/code-navigator` (TS 119파일) | 우리가 만든 웹앱판. `server/src/core/SymbolIndex.ts` 에 실제 `SymbolIndex` 가 있고 `SymbolIndexHolder` 도 있어 단어 경계가 같이 드러난다 |


목·단위 테스트가 통과해도 앱에서는 깨질 수 있다. 아래를 **실제 앱에서** 확인한다.

| 시나리오 | 확인할 것 |
|---|---|
| SC-1 | 프로젝트를 열고 심볼에 커서를 두고 정의 이동 → 선언 위치로 이동·강조 |
| SC-2 | 동명 함수 3곳 → 후보 3건 표시, 선택 시 이동 |
| SC-3 | 함수 추가 후 `:w` → 2초 내 심볼 검색에 등장 |
| SC-4 | 사용자 `~/.config/nvim` 설정(키맵)이 앱 안에서 그대로 동작 |
| SC-6 | 정규식 모드에 `([unclosed` → "잘못된 정규식" 에러(빈 결과가 아니라) |
| SC-7 | Neovim 프로세스 강제 종료 → "편집 세션 끊김" 안내 + 재기동 수단 |
| SC-9 | Vim 모드로 수정 → 표준 모드 전환 후에도 수정분·커서·undo 유지, ⌘Z 동작 |
| REQ-010 AC-2 (표준 모드) | **⌘Z 가 문자를 삽입하지 않는가 · ⌘S 가 실제로 디스크에 쓰는가 · ⌘[ 가 다음 타자를 먹지 않는가.** 실측으로 셋 다 깨져 있었다(아래) — 수정 후 반드시 재확인 |
| REQ-010 AC-1·2·3·5·6 | 키 입력·마우스 선택·모드 표시·토글·모드 복원 |

### 표준 모드 메뉴 명령 — 실측된 결함 (수정 후 재검증 필수)
`MenuCommandRouter` 가 메뉴 명령을 **raw 키 문자열**로 보낸다. Vim 모드에선 맞지만 표준 모드에선
nvim 이 **삽입 모드**라 같은 키가 다른 뜻이 된다. backend-junior 가 실제 nvim 으로 재측정했다:

| 메뉴 | 보내는 것 | 표준 모드 실측 결과 |
|---|---|---|
| ⌘Z 실행취소 | `u` | `line one` → **`uline one`** — 문자가 삽입된다 |
| ⌘S 저장 | `:w<CR>` | **디스크 변화 없음(저장 안 됨)** + 줄 분리 |
| ⌘[ 뒤로 | `<C-o>` | 삽입 모드에선 "다음 키 하나를 명령으로" 대기 — **사용자의 다음 타자를 먹는다** |

**⌘S 가 가장 나쁘다 — "저장했다고 믿는데 저장되지 않는" 형태라 데이터 유실이다.**

엔진에는 **모드 안전한 경로가 이미 있다**(`EditorSession.jumpBack()` 은 `nvim_command` 로 실행해
모드와 무관하다 · `NeovimStandardMode.commandShortcuts` 는 `<D-z>`→`<C-o>u`, `<D-s>`→`<C-o>:write<CR>`
를 이미 갖고 있다). 만들어 놓고 안 연결한 것이다.

## 4. 디자인 충실도

`_workspace/prototype/shots/` 를 시각 기준물로, 실제 앱 스크린샷과 화면별 대조
(`scripts/screenshot-app.sh`). 레이아웃·토큰·상태·반응형.

⚠ **`02_design.md` §4.1 대비 수치는 리더가 "실제 놓이는 표면 전부의 최악값" 기준으로 갱신했다.**
구현이 문서와 다르면 먼저 문서가 낡았는지 확인한다 — 무비판적으로 02 를 기준물로 쓰면 거짓 버그를 낸다.

## 5. 비기능 실측

### 이미 닫힌 것 (23:5x, backend-junior 실측 + 리더 확인)
| 항목 | 기준 | 실측 | 여유 |
|---|---|---|---|
| REQ-NF-001 전문 검색(리터럴, 전체 스캔) | ≤2000ms | **305ms** | 6.5배 |
| REQ-NF-001 전문 검색(정규식, 전체 스캔) | ≤2000ms | **250ms** | 8배 |
| REQ-NF-001 초기 인덱싱(5,000파일) | ≤10초 | 기존 스위트 통과 | — |
| REQ-NF-001 증분 갱신 | ≤500ms | 기존 스위트 통과 | — |
| REQ-NF-001 심볼 검색 | ≤100ms | 기존 스위트 통과 | — |
| REQ-NF-002 앱 번들 | ≤200MB | **11MB** | 18배 |

질의는 **레포 전체를 훑어야 하는 것**으로 골랐다. 흔한 단어는 상한 500건에 금방 닿아 일찍
멈추므로 최악을 재지 못한다 — 그건 따로 재서 상한이 실제로 작동하는지 확인했다.

### 화면 해제 직후 내가 돌릴 것 (backend-junior 가 준비해 둠)
```bash
_workspace/measure-app-runtime.sh              # REQ-NF-003 기동 시간이 이때 나온다
# 앱에서 중형 레포를 열고 인덱싱이 끝나면:
_workspace/measure-app-runtime.sh --idle-only  # 이것이 SC-8 판정값이다
```
스크립트는 `INCOMPLETE` 를 `PASS` 와 분리한다(종료코드 2). **"예산을 넘었다"와 "재지 못했다"는
다른 결론**이고, 후자가 인증 문서에 옮겨지면 "쟀다"로 굳는다. 자체 검사 5방향 통과 확인됨
(시계·메모리 측정기 200MB 프로브·창 탐지기 부재 판정·성공 분기·PASS 분기).

⚠ **앱에 프로젝트를 비대화식으로 여는 경로가 없다**(argv 핸들러·`application(_:open:)`·자동 복원
전부 부재 — 실측). 그래서 SC-8 은 **내가 화면에서 직접 프로젝트를 열어야만** 잴 수 있다.

### 아직 남은 것 — `.app` 의 실제 메모리
| 항목 | 기준 | 상태 |
|---|---|---|
| REQ-NF-002 · SC-8 유휴 메모리 | ≤150MB | 테스트 프로세스에선 **27.4MB**(인덱스 비용 19.1MB, 심볼 25,000). **`.app` 실측은 미완** |
| REQ-NF-003 기동 | ≤2초 | 실행부터 조작 가능까지 — `.app` 실측 필요 |

⚠ **테스트 프로세스의 숫자는 `.app` 의 숫자가 아니다.** SC-8 의 최종 인증은 실행 중인 앱에서 잰다.

### 측정에서 걸린 거짓 그린 둘 (같은 실수를 반복하지 않기 위해)
1. **측정 대상이 이미 해제돼 있었다.** 첫 측정값이 "인덱스 비용 0.3MB" 였는데, 25,000 심볼이
   그럴 리 없다. 원인은 마지막 사용 이후 **ARC 가 엔진을 먼저 해제**한 것 — 사라진 인덱스를
   재고 "메모리를 거의 안 쓴다"고 통과하고 있었다. `withExtendedLifetime` 으로 감싸니 20.1MB.
2. **같은 프로세스에서는 증가분이 0 으로 나온다.** 앞선 테스트가 잡아둔 페이지를 재사용하기
   때문이다(단독 20.1MB / 전체 스위트 안에서 0.0MB). 그래서 SC-8 은 게이트에서 **격리 실행**해
   출력 숫자로 판정한다.

→ 그리고 숫자를 믿기 전에 **측정기 자체를 검사**했다(50MB 를 실제로 할당·터치 → 관측 증가
50.0MB). **측정기가 죽어 있으면 150MB 예산 테스트는 무엇을 재든 영원히 통과한다.**

## 6. 증거로 인정하지 않는 것

- **"파일이 존재한다"** — 라우트 파일이 있고 등록도 됐는데 엔드포인트가 TODO 였던 사례가 있다.
- **"테스트가 전부 초록"** — 존재하는 테스트의 그린일 뿐이다. 뷰 테스트 0건인 상태에서도 630건이 초록이었다.
- **매트릭스의 `테스트 상태` PASS** — "나열된 테스트가 통과한다"는 뜻이지 화면 성립이 아니다.
- **`--self-check` 통과** — `CodeNavigatorMain.swift` 에서 `run()` **이전에 return** 한다. 창·뷰 생성을 실행하지 않는다.
- **팀의 자가 라이브 E2E·디자인 충실도 PASS** — 이 둘은 리더 인증 단계다. 팀이 돌렸으면 "팀 자가검증(리더 인증 대기)"로만 표기한다.
- **생산 쪽 참조만 세는 것** — 값을 *쓰는* 호출부가 있으면 grep 은 "쓰이고 있음"으로 읽는다.
  실측: `MenuCommandRouter` 가 `setTreeVisible`/`setPanelVisible` 을 부르고 `ShellPreferences` 가
  저장·복원까지 하는데 **그 값을 읽는 쪽이 없어** 메뉴는 반응하고 화면은 안 바뀌었다.
  미구현보다 이 상태가 덜 보인다 — 호출부가 있으니 연결된 것처럼 읽힌다.
  **소비처까지 세어야 연결 확인이다.**
- **단위 테스트 통과를 요구사항 충족으로 읽는 것** — 모듈은 *자기가 약속한 것*을 지키고,
  요구사항이 약속한 것은 **조합해 봐야** 드러난다. 실측: `PopoverPlacement` 는 "어느 쪽에도
  자리가 없으면 영역 안에 가둔다"를 지켰고 그 테스트도 통과했는데, 낮은 창에서 그 결과가
  **커서 줄을 덮었다**(카드 `(80,8,460×308)` 가 앵커 `(80,90,8×18)` 를 가림). 02 §3 W-4 의
  "팝오버가 커서 줄을 절대 덮지 않는다"가 깨지는데 단위 테스트는 초록이었다.
  → **모듈의 계약과 요구사항의 계약은 다른 문서다.** 배선한 뒤 산술을 따라가야 드러난다.
- **테스트 참조를 소비로 세는 것** — 이게 "존재≠완료"의 **가장 조용한 형태**다. 실측:
  `PopoverPlacement` 는 참조 11건이라 쓰이는 것처럼 보였는데 **전부 테스트였고 소스 참조는 0**이었다.
  그래서 팝오버 플립·"커서 줄을 덮지 않는다"가 구현돼 있지 않은데 모듈은 완전히 테스트돼 있다.
  **모듈은 완전히 테스트되면서 동시에 완전히 고아일 수 있고, 테스트가 그 사실을 가린다.**
  커버리지 매트릭스가 PASS 로 가득한데 화면에 그 기능이 없는 상황이 정확히 이렇게 만들어진다.
  → 세는 범위를 **`Sources/` 와 `Tests/` 로 나눠서** 봐라.
- **이름이 완전성을 주장하는 테스트** — `ShellCompositionTests` 의 "만들어 놓고 안 쓰는 것이 없다"가
  통과하는 동안 `ShellSplitter` 는 참조 0이었다. 그 테스트는 `ShellPane.allCases` 만 훑는데,
  스플리터는 "영역"이 아니라 목록에 없었다. **이름이 완전성을 주장하면 다음 사람은 그 뒤를 안 본다.**
- **빈 명령 출력** — 그 명령이 잡을 수 있는지부터 확인한다. 리더가 `ioreg` 빈 결과를 "잠금 해제"로 오독했고,
  스크린샷 파일 크기를 "찍힘"의 증거로 삼았다(내용은 배경화면이었다). 미마운트를 세는 임시
  스크립트가 셸 문법 오류로 매 루프 실패하면서 "22종 전부 미마운트"라는 그럴듯한 경보를 내기도
  했다. **두 측정이 반대를 가리키면 먼저 측정기를 의심한다.**
