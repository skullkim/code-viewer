# RESUME — 실행 · 터미널 · 실행 설정

**상태: 완료**

> 사용자 지시: "디버그 실행할 수 있는 버튼 이런거 다 있지? 이제 터미널도 붙여서 서버 실행
> 할 수 있게 해봐. 실행할때 환경변수 넣을 수 있는 세팅도 만들어."
>
> 첫 물음의 답은 **아니오** 였다 — 지금은 이미 떠 있는 JVM 에 붙는 것만 된다. 실행 버튼도
> 터미널도 없다. 셋 다 만든다.
>
> 완료 판정은 리더가 `_workspace/RUN_COMPLETE.md` 를 쓰는 시점이다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 실행 설정 모델 (명령·작업 디렉터리·환경변수) + 저장 | ✅ |
| 2 | 터미널 패널 (실제 셸이 도는 것) | ✅ |
| 3 | 실행 버튼 — 설정대로 터미널에서 돌린다 | ✅ |
| 4 | 디버그 실행 — JDWP 인자를 붙여 띄우고 자동으로 붙는다 | ✅ |
| 5 | 실행 설정 편집 화면 (환경변수 표) | ✅ |
| 6 | v0.5 릴리스 | ✅ |

## 실측으로 잡은 결함 (테스트만으로는 안 나왔다)

| # | 증상 | 원인 | 어떻게 잡았나 |
|---|---|---|---|
| 1 | 서버 기동 로그가 12글자만 남는다 | 그리드 뷰가 첫 레이아웃에서 1×1 을 보고 → libvterm 이 화면을 한 칸으로 줄이며 찍힌 줄을 파괴 | 실제 앱에 탐침을 심어 `resize 1x1` 을 눈으로 봤다. 단위 테스트는 전부 초록이었다 |
| 2 | 디버그 실행을 눌러도 서버가 안 뜬다 | `suspend=y` 로 띄우고 붙기만 하고 재개하지 않았다 | 화면에 `Listening for transport` 만 있고 `PORT=` 가 없는 것을 보고 |
| 3 | 셸 버튼이 아무 일도 안 한다 | 프로젝트가 없으면 `guard … else { return }` 로 조용히 끝났다 | 코드를 읽다 발견. 상태 메시지로 바꿨다 |

**검사기가 틀린 적 3회** — (a) `--filter` 는 표시 이름이 아니라 타입 이름을 본다(새 스위트가
"0 tests passed" 로 보였다) (b) 접근성 `set value` 는 SwiftUI 바인딩을 건드리지 않는다(앱이
저장을 안 한 것처럼 보였다) (c) AppleScript `entire contents` 는 큰 창에서 조용히 0건을
돌려준다(창에 버튼이 하나도 없는 것처럼 보였다). 셋 다 앱을 고치기 전에 검사기를 먼저
의심해서 갈랐다.

## 설계 결정

**터미널은 nvim 의 `:terminal` 을 쓴다.** 우리가 VT100 파서와 PTY 를 직접 쓰지 않는다.
이미 nvim 을 번들하고 있고, nvim 의 터미널은 libvterm 기반의 진짜 터미널 에뮬레이터다.
UI 프로토콜도 우리가 이미 다룬다 — 그리드 렌더러·키 입력·마우스가 그대로 재사용된다.

값은 **별도 nvim 프로세스**다. 편집기와 같은 인스턴스를 쓰면 터미널 버퍼가 편집기 자리를
차지하고, 탭 전환이 서로를 밀어낸다.

터미널 nvim 은 `--clean` 으로 띄운다. 편집기는 INV-4 때문에 사용자 설정을 읽지만, 터미널은
사용자 플러그인이 키를 가로채면 셸이 안 먹는다 — 예측 가능한 쪽이 낫다.

**디버그 실행은 명령을 고쳐 쓰지 않는다.** `-agentlib:jdwp=...` 를 어디에 넣을지는
명령마다 다르다(`java` 직접 · `./gradlew bootRun` · `mvn`). 대신 **`JAVA_TOOL_OPTIONS`
환경변수**로 넣는다 — JVM 이 표준으로 읽고, 어떤 실행 방식이든 통한다.

## 규율

TDD · 조용한 실패 의심 · 판정에 `2>/dev/null` 금지 · 0건은 positive control 뒤에 ·
게이트는 조용한 창에서 · **관측 도구를 먼저 의심하라**.

GUI 조작에서 배운 것: 모달에는 `postToPid` 가 안 닿는다(접근성으로 값을 넣어라) · 모달이
떠 있을 때만 창 열거가 된다 · GUI 조작에도 `2>/dev/null` 을 쓰지 마라.

## 이어받는 절차

```bash
cd /Users/skull/Documents/repo/code-navigator-mac
git log --oneline -5
swift test --no-parallel > /tmp/t.log 2>&1; grep -E "✘|Test run with" /tmp/t.log
./_workspace/gate.sh
```
