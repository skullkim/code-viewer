/** 한글이 섞인 줄에서 커서·셀 폭이 어긋나지 않는지 눈으로 확인하는 자리. */
// 이 줄의 목적은 '붙어 있는 이름은 검색에 걸리지 않는다'를 보이는 것이다.
// 그러니 이 파일 안에 검색어와 같은 토큰을 따로 떼어 두지 마라(주석도 포함이다 —
// 검색은 주석과 코드를 구분하지 않는다). 그런 토큰이 있으면 그 줄이 정당하게 일치해서
// 경계 규칙이 깨진 것처럼 보인다.
export const 안내문 = '사용자Index 는 부분 단어라 검색에 걸리면 안 된다';

export function 요약하다(문장: string): string {
  return 문장.slice(0, 10) + '…';
}
