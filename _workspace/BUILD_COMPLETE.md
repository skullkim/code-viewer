# BUILD_COMPLETE — code-navigator-mac

**인증 시각**: 2026-09-01 (리더 독립 검증 후 작성)
**인증 트리**: `79241c4` — Sources·Tests·scripts·gate.sh 기준
**인증 번들**: `.build/bundles/CodeNavigator-20260831-203757.app` (`clean: yes`)

> 이 파일의 존재가 '완료'의 유일한 신호다. 매트릭스·QA 리포트의 완료 문구는
> 리더 인증 대기 중인 주장이었고, 이 파일이 그것을 종결한다.

## 리더 독립 검증

**풀게이트 연속 3회 — 같은 소스 트리에서**
```
1회차  PASS  1,407건 / 179 스위트  SIGSEGV 0
2회차  PASS  1,407건 / 179 스위트  SIGSEGV 0
3회차  PASS  1,407건 / 179 스위트  SIGSEGV 0
조용한 창: 미커밋 0 · 변이 0 · 프로브 0 · 앱 0 · nvim 0 · .build 잡은 프로세스 0
```
세 번 다 같은 수다. 앞선 시도에서 3회차가 **1,375 대 1,376** 한 건 차이로 무효가 됐고,
그것이 이 기준이 필요한 이유였다.

**게이트가 검사한 것**: 뷰 31종 전부 마운트·플레이스홀더 0·**면제 0** · 포커스 대칭 + 도달성 ·
자체 검사 3종 · 입력 소스 선택 호출 0건(REQ-014 AC-4) · 유휴 메모리 31MB/150MB ·
탭 여닫기 672KB < 인덱스 하나 4,608KB · 고아 nvim 0 · 민감정보 스캔 426건 대상

**리더가 직접 대조한 것**: 디자인 충실도 — `prototype/shots/render-markdown-large.png` ↔
`shots-live/render-markdown-narrow.png`. 일치·불일치를 내 눈으로 갈랐고 불일치 3건은 수정됐다.

## 사용자 요구 3종

| | 상태 |
|---|---|
| **여러 프로젝트를 탭으로** (REQ-012) | ✅ 라이브 — 탭 2개·즉시 전환·인덱스 생존·탭별 검색 범위·프로젝트 격리 양방향·미저장 표시 |
| **markdown·html 렌더** (REQ-013) | ✅ 라이브 — `.md`/`.html` 각각·폭 727px·코드블록 자체 스크롤·링크 밑줄·차단 3분류. **편집 세션은 살아 있다**(요구 문장의 "도") |
| **한글 자판 Vim 명령키** (REQ-014) | 🟡 **부분** — 영문 경로 라이브 통과. **한글 자판 실측 미완** (아래) |

## ⏸ 미검증 — 명시한다

**한글 조합 중 4칸 (인증 시점 미검증)**
```
조합 중 ← →      IME 가 조합 안에서 쓰는가
조합 중 delete   자모 하나만 지우는가
조합 중 Enter    음절이 남고 줄바꿈되는가
조합 중 Home     음절이 남고 이동하는가
```
**코드로는 처리했고 테스트도 있다. 라이브로는 아무도 못 봤다.**

⚠ **그리고 이것은 이 빌드가 만든 위험이다.** 방향키·delete 를 표기 경로로 옮기면서
조합 중 처리를 표에 적었고, 그 열은 한 칸도 라이브에 서지 않았다. 틀렸다면
**사용자가 치던 음절이 사라진다 — 오늘 고친 어떤 결함보다 되돌리기 어렵다.**

**왜 못 쟀나**: 합성 입력이 *어느 창이 앞에 있는가*에 의존하는데 통제하지 못했다.
QA 3회·리더 1회·스크립트 1회 시도 전부 같은 벽. 한글 입력 소스 전환은 적용될 때도
안 될 때도 있고, `TISSelectInputSource` 로 강제하는 것은 **REQ-014 AC-4 의 방어선을
검증 도구가 깨는 것**이라 하지 않았다.

**재는 법**: `bash _workspace/verify-korean-chords.sh` (인자 없으면 최신 번들). 사람 손 ~1분.

**그 외 미검증(측정 불가)**: 인덱싱 `◌` · SC-4 · REQ-010 AC-6 · REQ-007 AC-3 ·
REQ-010 AC-2 · 창 크기/분할 복원

## 열린 결함

**0건.**

## 인증 후 처리 목록 (우선순위 순)

```
1. bufferLines 의 세 개의 0 — compactMap 제거 + .binary 를 읽는 textValue   오분류
2. invalidPath 분류 — 계약 오용 vs 보안 거절                                오분류
3. dirtyFiles·saveAll 프로토콜 승격 + 앱 재배선                              구조
4. tabs() 단일 소스 ADR (ADR-0111 A안) — 트리↔인덱스 실측을 근거로 추가       구조
5. sharedModel 조용한 무동작 · 프리젠테이션 필드 3건 View 미참조             점검
6. 폴더 라벨 클릭 UX(PD 판단) · reorderTabs 범위 크리프 · 부채-6 조합 중 표시
```
**1·2 가 3·4 보다 위다 — 구조는 개발자를 헷갈리게 하고 오분류는 사용자에게 거짓을 말한다.**

## 오늘의 단일 최대 신호

**"테스트가 초록인데 기능이 죽어 있다"가 여덟 번.** 그중 셋(D-19b·D-21·방향키)은
`route()` 의 같은 한 줄 — `return .interpretForComposition` — 로 떨어졌다.

> **그 줄이 "나머지 전부"를 받는데, 그 나머지에 무엇이 있는지 한 번도 세지 않았다.**

그리고 **마지막 두 개(방향키·Enter)는 사용자가 찾았다.** 라이브 검증 여섯 번, 게이트 31종,
테스트 1,407건이 돌았는데 **아무도 방향키를 눌러보지 않았다.**
